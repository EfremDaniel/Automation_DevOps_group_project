name: Exercise_3

on:
  workflow_dispatch:

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      greeting: ${{ steps.set.outputs.greeting }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set greeting output
        id: set
        run: |
          echo "greeting=Hej från prepare-jobbet" >> $GITHUB_OUTPUT

  test:
    needs: prepare
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Echo greeting
        run: |
          echo "GREETING=$GREETING"
          echo "Using secret: ${SECRET_VAL:+exists}"
